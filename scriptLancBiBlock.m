n=1000;
m=20;
% x0=zeros(n,1);
A=2*eye(n)-diag(ones(n-1,1),-1)-diag(ones(n-1,1),+1);
%b=A*ones(n,1);
%r0=b-A*x0;
%[H,V]=NVarnoldi(A,b,m);
%[H,V]=NVarnoldiModifie(A,b,m);
%Hm=H(1:m,:);
%Vm=V(:,1:m);
%Im=eye(m);
%erreur1=norm(A*Vm-V*H,'fro')
%erreur2=norm(A*Vm-V*H,'fro');
%[X,erreur]=NVFom(A,r0,m)
%[X,erreur,k]=NVFomModifie(A,r0,m);
%[X]=NVLancSymG(A,r0,m);
 b1=A*ones(n,1);
 b2=7*(A*ones(n,1));
 b3=17*(A*ones(n,1));
 p=3;
 B=10*rand(n,p);
 X0=ones(n,p);
 R0=B-A*X0;
 %R0=B-A*X0;
%[Hm,Vm]=Arlondi_par_bloc(A,X0,m,B,p);
%[H,V]=NVArnoldiGlobal(A,X0,m,B);
  c1=3*(A'*ones(n,1));
  c2=4*(A'*ones(n,1));
  c3=5*(A'*ones(n,1));
  C=ones(n,p);
  Rt0=C-A'*X0;
  erreur=NVLancBlocBI(A,B,C,X0,m,p,R0,Rt0);
